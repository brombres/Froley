augment RogueTemplate
  PROPERTIES
    cmd_init_args =
      @|module RogueFroley
       |
       |class CmdInitArgs : Cmd[]
       |  PROPERTIES
       |    t : Token
       |
       |  METHODS
       |    method require_count( required_count:Int32 )
       |      if (count != required_count)
       |        throw Error( "[]", "init(t,args) got $, expected $." ("# arg".pluralized(count),required_count) )
       |      endIf
       |endClass
       |
endAugment

