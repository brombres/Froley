augment RogueTemplate
  PROPERTIES
    cmd_init_args =
      @|class RogueFroleyCmdInitArgs : RogueFroleyCmd[]
       |  PROPERTIES
       |    t : RogueFroleyToken
       |
       |  METHODS
       |    method require_count( required_count:Int32 )
       |      if (count != required_count)
       |        throw RogueFroleyError( "[RogueFroley]", "init(t,args) got $, expected $." ("# arg".pluralized(count),required_count) )
       |      endIf
       |endClass
       |
endAugment

